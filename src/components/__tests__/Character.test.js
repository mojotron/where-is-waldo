import { render, screen } from "@testing-library/react";
import Character from "../Character";

describe("Character component", () => {
  test("renders character icon", () => {});
  test("renders character name", () => {});
});
