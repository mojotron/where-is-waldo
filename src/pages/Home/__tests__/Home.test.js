import { render, screen } from "@testing-library/react";
import Home from "../Home";

describe("Home component", () => {
  test("getting level documents", () => {
    render(<Home />);
  });
  test("renders list", () => {});
  test("renders level cards", () => {});

  // integration
  // test clicking level card
  // test clicking navigation
  // test level click
});
